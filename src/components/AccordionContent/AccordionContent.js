import styled from 'styled-components'

const AccordionContent = styled.View((props) => ({
  paddingTop: props.theme.spacing(1),
  paddingBottom: props.theme.spacing(2),
}))

export default AccordionContent
